PORT=
MONGODB_URI=
JWT_SECRET=
JWT_EXPIRES_IN=
NODE_ENV=

# CORS — set to your frontend URL in production (e.g. https://myapp.vercel.app)
# Leave unset in development to allow all origins
ALLOWED_ORIGIN=
